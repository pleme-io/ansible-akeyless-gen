---
# Integration test for gateway_log_forwarding_logz_io

- name: Test gateway_log_forwarding_logz_io module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create gateway_log_forwarding_logz_io
      gateway_log_forwarding_logz_io:
        state: present
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create gateway_log_forwarding_logz_io (idempotent)
      gateway_log_forwarding_logz_io:
        state: present
      register: idempotent_result

    - name: Delete gateway_log_forwarding_logz_io
      gateway_log_forwarding_logz_io:
        state: absent
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
