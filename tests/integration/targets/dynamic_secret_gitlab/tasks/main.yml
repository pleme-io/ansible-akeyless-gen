---
# Integration test for dynamic_secret_gitlab

- name: Test dynamic_secret_gitlab module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create dynamic_secret_gitlab
      dynamic_secret_gitlab:
        state: present
        gitlab_access_type: "test_value"
        gitlab_token_scopes: "test_value"
        name: "test_value"
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create dynamic_secret_gitlab (idempotent)
      dynamic_secret_gitlab:
        state: present
        gitlab_access_type: "test_value"
        gitlab_token_scopes: "test_value"
        name: "test_value"
      register: idempotent_result

    - name: Delete dynamic_secret_gitlab
      dynamic_secret_gitlab:
        state: absent
        gitlab_access_type: "test_value"
        gitlab_token_scopes: "test_value"
        name: "test_value"
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
