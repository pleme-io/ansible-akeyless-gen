---
# Integration test for gateway_log_forwarding_datadog

- name: Test gateway_log_forwarding_datadog module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create gateway_log_forwarding_datadog
      gateway_log_forwarding_datadog:
        state: present
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create gateway_log_forwarding_datadog (idempotent)
      gateway_log_forwarding_datadog:
        state: present
      register: idempotent_result

    - name: Delete gateway_log_forwarding_datadog
      gateway_log_forwarding_datadog:
        state: absent
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
