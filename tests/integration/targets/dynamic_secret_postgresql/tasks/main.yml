---
# Integration test for dynamic_secret_postgresql

- name: Test dynamic_secret_postgresql module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create dynamic_secret_postgresql
      dynamic_secret_postgresql:
        state: present
        name: "test_value"
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create dynamic_secret_postgresql (idempotent)
      dynamic_secret_postgresql:
        state: present
        name: "test_value"
      register: idempotent_result

    - name: Delete dynamic_secret_postgresql
      dynamic_secret_postgresql:
        state: absent
        name: "test_value"
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
