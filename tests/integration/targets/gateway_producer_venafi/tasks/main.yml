---
# Integration test for gateway_producer_venafi

- name: Test gateway_producer_venafi module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create gateway_producer_venafi
      gateway_producer_venafi:
        state: present
        name: "test_value"
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create gateway_producer_venafi (idempotent)
      gateway_producer_venafi:
        state: present
        name: "test_value"
      register: idempotent_result

    - name: Delete gateway_producer_venafi
      gateway_producer_venafi:
        state: absent
        name: "test_value"
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
