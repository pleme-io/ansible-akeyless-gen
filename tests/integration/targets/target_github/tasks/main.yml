---
# Integration test for target_github

- name: Test target_github module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create target_github
      target_github:
        state: present
        name: "test_value"
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create target_github (idempotent)
      target_github:
        state: present
        name: "test_value"
      register: idempotent_result

    - name: Delete target_github
      target_github:
        state: absent
        name: "test_value"
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
