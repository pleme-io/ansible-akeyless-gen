---
# Integration test for gateway_log_forwarding_stdout

- name: Test gateway_log_forwarding_stdout module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create gateway_log_forwarding_stdout
      gateway_log_forwarding_stdout:
        state: present
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create gateway_log_forwarding_stdout (idempotent)
      gateway_log_forwarding_stdout:
        state: present
      register: idempotent_result

    - name: Delete gateway_log_forwarding_stdout
      gateway_log_forwarding_stdout:
        state: absent
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
