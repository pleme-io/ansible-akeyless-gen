---
# Integration test for target_gcp

- name: Test target_gcp module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create target_gcp
      target_gcp:
        state: present
        name: "test_value"
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create target_gcp (idempotent)
      target_gcp:
        state: present
        name: "test_value"
      register: idempotent_result

    - name: Delete target_gcp
      target_gcp:
        state: absent
        name: "test_value"
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
